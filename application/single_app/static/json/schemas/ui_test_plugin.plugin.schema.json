{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "UI Test Plugin",
  "description": "NYI-Schema for UI Test Plugin configuration, restricting auth.type to user, key, connection_string, and identity.",
  "allOf": [
    { "$ref": "plugin.schema.json" },
    {
      "type": "object",
      "properties": {
        "auth": {
          "type": "object",
          "properties": {
            "type": {
              "type": "string",
              "enum": ["user", "key", "connection_string", "identity"],
              "description": "Allowed values for UI Test Plugin: user, key, connection_string, identity."
            }
          }
        }
      }
    }
  ]
}